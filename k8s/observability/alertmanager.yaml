# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: alertmanager-config
#   namespace: shopmicro
# data:
#   alertmanager.yml: |
#     global:
#       slack_api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK' # <-- PASTE YOUR URL HERE

#     route:
#       group_by: ['alertname']
#       group_wait: 10s
#       group_interval: 5m
#       repeat_interval: 1h
#       receiver: 'slack-notifications'

#     receivers:
#       - name: 'slack-notifications'
#         slack_configs:
#           - channel: '#production-alerts'
#             send_resolved: true
#             title: '[{{ .Status | toUpper }}] {{ .GroupLabels.alertname }}'
#             text: >-
#               {{ range .Alerts }}
#                 *Alert:* {{ .Annotations.summary }}
#                 *Description:* {{ .Annotations.description }}
#                 *Runbook:* {{ .Annotations.runbook_url }}
#               {{ end }}
# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: alertmanager
#   namespace: shopmicro
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: alertmanager
#   template:
#     metadata:
#       labels:
#         app: alertmanager
#     spec:
#       containers:
#         - name: alertmanager
#           image: prom/alertmanager:latest
#           args:
#             - "--config.file=/etc/alertmanager/alertmanager.yml"
#           ports:
#             - containerPort: 9093
#           volumeMounts:
#             - name: alertmanager-config
#               mountPath: /etc/alertmanager
#       volumes:
#         - name: alertmanager-config
#           configMap:
#             name: alertmanager-config
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: alertmanager
#   namespace: shopmicro
# spec:
#   selector:
#     app: alertmanager
#   ports:
#     - port: 9093
#       targetPort: 9093
